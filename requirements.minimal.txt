# KMBlog Manager - 最小化依赖
# 只包含实际使用的库

# Core Framework
flet==0.80.2

# Image Processing
pillow==12.1.0

# Cryptography
cryptography==46.0.3

# HTTP & API
requests==2.32.5
fastapi==0.128.0
uvicorn==0.40.0
python-multipart==0.0.22

# YAML & Config
pyyaml==6.0.3

# PyInstaller (仅用于打包，不会被打包进 exe)
pyinstaller==6.18.0
